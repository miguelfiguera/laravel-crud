# Laravel + React starter kit (ReactJS, InertiaJS, Tailwind)

This is just a test.

Run php artisan db:seed --class=ProfileSeeder so database gets some fake initial data.

Quite hardcore to understand.
